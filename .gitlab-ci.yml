stages:
  - build-upload

variables:
  R2_BUCKET: blossom-cdn
  R2_ENDPOINT: https://7ad15a3d6489a22ad3c2c2fc2ed258db.eu.r2.cloudflarestorage.com

workflow:
  rules:
    - if: $CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"
    - if: $CI_PIPELINE_SOURCE == "schedule"
    - if: $CI_PIPELINE_SOURCE == "web"

build-upload-main:
  stage: build-upload
  image: ghcr.io/ublue-os/devcontainer:latest
  tags:
    - bigboi-docker-priv
  script:
    - just build-iso blossomos main main 0 0
    - just upload-r2 main

build-upload-nvidia-open:
  stage: build-upload
  image: ghcr.io/ublue-os/devcontainer:latest
  tags:
    - bigboi-docker-priv
  script:
    - just build-iso blossomos main nvidia-open 0 0
    - just upload-r2 nvidia-open

build-upload-nvidia-legacy:
  stage: build-upload
  image: ghcr.io/ublue-os/devcontainer:latest
  tags:
    - bigboi-docker-priv
  script:
    - just build-iso blossomos main nvidia-legacy 0 0
    - just upload-r2 nvidia-legacy

build-upload-netinstall:
  stage: build-upload
  image: ghcr.io/ublue-os/devcontainer:latest
  tags:
    - bigboi-docker-priv
  script:
    - just build-iso blossomos main main 0 1
    - just upload-r2 netinstall
